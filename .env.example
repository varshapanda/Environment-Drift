# Copy this file to .env and fill in real values. Never commit .env to Git.

DATABASE_URL=postgres://user:password@localhost:5432/driftlab
JWT_SECRET=replace-with-a-real-secret
PORT=3000
NODE_ENV=development
